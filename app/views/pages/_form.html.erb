<%= form_with model: Tweet.new do |form| %>
    <div class="field">
        <%= form.label :content %>
        <%= form.text_field :content %>
    </div>

    <div class="actions">
        <%= form.submit(class: "btn btn-primary") %>
    </div>
<% end %>